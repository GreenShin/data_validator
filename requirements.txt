# Core dependencies for CSV validation
pandas>=1.5.0          # CSV 파일 처리
numpy>=1.21.0          # 수치 계산 및 통계 (분포 분석용)
PyYAML>=6.0            # YAML 설정 파일 파싱
pydantic>=2.0          # 데이터 검증 및 모델링
click>=8.0             # 명령행 인터페이스
rich>=13.0             # 콘솔 출력 및 진행률 표시
python-dateutil>=2.8   # 날짜/시간 처리
email-validator>=2.0   # 이메일 형식 검증
phonenumbers>=8.13     # 전화번호 형식 검증
chardet>=5.0           # 문자 인코딩 감지
psutil>=5.9.0          # 시스템 및 프로세스 모니터링 (메모리 사용량 확인용)
